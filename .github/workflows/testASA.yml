name: LLDASATest

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: windows-2016
    steps:

      - name: checkout
        uses: actions/checkout@v2          

      - name: install dependencies
        run: |
          echo chg

      - name: test ASA switch
        id: testASA
        run: lld --add-stdcall-alias
