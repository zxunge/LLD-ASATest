name: LLDASATest

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: windows-latest
    steps:

      - name: checkout
        uses: actions/checkout@v2          

      - name: install dependencies
        run: |
          

      - name: test ASA switch
        id: testASA
        run: |
          lld-link --version
          lld-link --add-stdcall-alias
